# elal-voice-agent

FastAPI service connecting Airtable, Vapi, and end-of-call webhooks for the El Al
Hebrew-speaking delayed-flight voice agent.

> This README is filled in incrementally as each component lands (see project git history).
